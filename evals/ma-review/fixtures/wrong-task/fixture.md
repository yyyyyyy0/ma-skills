# Wrong task fixture

The user asks for a brand-new UI implementation, not for a review:

> 単に新しいUIを設計して実装してください。

`ma-review` should refuse to act as a design/build skill and should point the request back to `ma`.
